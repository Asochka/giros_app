<resources>
    <!-- Base application theme. -->
    <style name="Theme.Giros_app" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/dark_mode</item>
        <!--<item name="android:colorBackground">@color/dark_grey</item> -->
        <item name="android:textColor">@color/white</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_pink</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <!-- text inside the button -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="textInputLayoutBackground">@color/dark_mode</item>
        <item name="cardViewBackground">@color/dark_mode_card</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>